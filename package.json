{
  "name": "studentwell-monorepo",
  "version": "1.0.0",
  "description": "StudentWell — multi-tenant student mental-health and academic wellness platform.",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev": "npx concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "build:server": "npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=client",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=client",
    "test": "npm run test --workspace=server",
    "audit": "npm audit --workspaces --include-workspace-root"
  }
}
